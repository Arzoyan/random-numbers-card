import React from 'react';

const Instructions = () => {
  return (
    <div className="instructions">
      <h2>Instructions</h2>
      <p>Add cards with random numbers, and sort or delete them using the buttons.</p>
    </div>
  );
};

export default Instructions;
